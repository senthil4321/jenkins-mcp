{
  "inputs": [
    {
      "name": "query",
      "type": "promptString",
      "description": "The string input to send to the MCP server"
    }
  ],
  "servers": {
    "jenkins_mcp": {
      "command": ".venv/Scripts/python.exe",
      "args": ["hello_mcp.py"]
    },
    "linux_mcp": {
      "command": ".venv/Scripts/python.exe",
      "args": ["ssh_mcp.py"]
    }
   }
}
